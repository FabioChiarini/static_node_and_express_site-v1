extends layout.pug

block content 
  article.grid-container.portfolio-intro
    div

      h1 My Portfolio

      p.lead.text-light
        | This portfolio is a collection of the various projects i made during the TeamTreeHouse Full Stack Javascript course
  article.grid-container.portfolio-index
    
    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3

      //- display images, title and add href to each project passed in from app.js   
      each project in projects
        .cell
          a(href='./projects/' + project.id)
            img.thumbnail(src=project.image_urls[0])
            h5= project.project_name

      //- Use a combination of interpolation and template literals as needed to dynamically generate the the routes in the href
